'use client';

import { Canvas, type ThreeEvent, useFrame, useThree } from '@react-three/fiber';
import { useRouter } from 'next/navigation';
import { useEffect, useMemo, useRef, useState } from 'react';
import * as THREE from 'three';
import type { LibraryVolume } from '../../lib/library-scene';
import styles from './three-bookshelf.module.css';

type ThreeBookshelfProps = { volumes: LibraryVolume[] };
type ShelfBook = { volume: LibraryVolume; height: number; coverWidth: number; thickness: number; shelfX: number; shelfZ: number };
const DEG = Math.PI / 180;
const SHELF_TOP = .035;
function presentationArtUrl(volume: LibraryVolume) { return volume.coverArtUrl ?? volume.representativeImageUrl; }
function canvasTexture(canvas: HTMLCanvasElement) { const texture = new THREE.CanvasTexture(canvas); texture.colorSpace = THREE.SRGBColorSpace; texture.anisotropy = 16; texture.minFilter = THREE.LinearMipmapLinearFilter; texture.magFilter = THREE.LinearFilter; texture.generateMipmaps = true; texture.needsUpdate = true; return texture; }
function makeSpineTexture(volume: LibraryVolume) { const canvas = document.createElement('canvas'); canvas.width = 384; canvas.height = 1400; const context = canvas.getContext('2d'); if (!context) return null; context.fillStyle = volume.spineColor; context.fillRect(0,0,canvas.width,canvas.height); const light=context.createLinearGradient(0,0,canvas.width,0); light.addColorStop(0,'rgba(255,255,255,.075)'); light.addColorStop(.32,'rgba(255,255,255,.02)'); light.addColorStop(.68,'rgba(0,0,0,.012)'); light.addColorStop(1,'rgba(0,0,0,.105)'); context.fillStyle=light; context.fillRect(0,0,canvas.width,canvas.height); context.globalAlpha=.045; context.strokeStyle='#fff'; context.lineWidth=1; for(let x=6;x<canvas.width;x+=10){context.beginPath();context.moveTo(x,0);context.lineTo(x+1,canvas.height);context.stroke();} context.globalAlpha=.03; context.strokeStyle='#000'; for(let y=6;y<canvas.height;y+=9){context.beginPath();context.moveTo(0,y);context.lineTo(canvas.width,y+1);context.stroke();} context.globalAlpha=1; context.fillStyle=volume.bandColor; context.globalAlpha=.56; context.fillRect(0,172,canvas.width,8); context.fillRect(0,canvas.height-180,canvas.width,8); context.globalAlpha=1; context.save(); context.translate(canvas.width/2,canvas.height/2); context.rotate(Math.PI/2); context.textAlign='center'; context.textBaseline='middle'; context.fillStyle=volume.inkColor; let fontSize=48; context.font=`${fontSize}px Georgia, serif`; while(fontSize>25&&context.measureText(volume.title).width>1000){fontSize-=2;context.font=`${fontSize}px Georgia, serif`;} context.fillText(volume.title,0,-3,1020); context.font='19px Arial, sans-serif'; context.globalAlpha=.64; context.fillText(volume.creator.toUpperCase(),0,80,820); context.restore(); return canvasTexture(canvas); }
function drawWrappedTitle(context:CanvasRenderingContext2D,title:string,centerX:number,startY:number,maxWidth:number){const words=title.toUpperCase().split(/\s+/);const lines:string[]=[];let current='';for(const word of words){const candidate=current?`${current} ${word}`:word;if(context.measureText(candidate).width<=maxWidth||current.length===0)current=candidate;else{lines.push(current);current=word;}}if(current)lines.push(current);lines.slice(0,4).forEach((line,index)=>context.fillText(line,centerX,startY+index*74,maxWidth));}
function makeFallbackCoverTexture(volume:LibraryVolume){const canvas=document.createElement('canvas');canvas.width=768;canvas.height=1152;const context=canvas.getContext('2d');if(!context)return null;context.fillStyle=volume.spineColor;context.fillRect(0,0,canvas.width,canvas.height);const vignette=context.createRadialGradient(390,510,80,390,510,640);vignette.addColorStop(0,'rgba(255,255,255,.05)');vignette.addColorStop(1,'rgba(0,0,0,.09)');context.fillStyle=vignette;context.fillRect(0,0,canvas.width,canvas.height);context.strokeStyle=volume.bandColor;context.globalAlpha=.5;context.lineWidth=3;context.strokeRect(50,52,canvas.width-100,canvas.height-104);context.globalAlpha=1;context.fillStyle=volume.inkColor;context.font='48px Georgia, serif';context.textAlign='center';context.textBaseline='top';drawWrappedTitle(context,volume.title,canvas.width/2,160,610);context.font='21px Arial, sans-serif';context.globalAlpha=.68;context.fillText(volume.creator.toUpperCase(),canvas.width/2,980,590);context.globalAlpha=1;return canvasTexture(canvas);}
function useSpineTexture(volume:LibraryVolume){const[texture,setTexture]=useState<THREE.Texture|null>(null);useEffect(()=>{const next=makeSpineTexture(volume);setTexture(next);return()=>next?.dispose();},[volume.bandColor,volume.creator,volume.inkColor,volume.spineColor,volume.title]);return texture;}
function useFallbackCoverTexture(volume:LibraryVolume){const[texture,setTexture]=useState<THREE.Texture|null>(null);useEffect(()=>{const next=makeFallbackCoverTexture(volume);setTexture(next);return()=>next?.dispose();},[volume.creator,volume.inkColor,volume.spineColor,volume.title,volume.bandColor]);return texture;}
function useCoverTexture(url:string|null){const[texture,setTexture]=useState<THREE.Texture|null>(null);useEffect(()=>{setTexture(null);if(!url)return undefined;let disposed=false;let loaded:THREE.Texture|null=null;const loader=new THREE.TextureLoader();loader.load(url,(next)=>{loaded=next;if(disposed){next.dispose();return;}next.colorSpace=THREE.SRGBColorSpace;next.anisotropy=16;next.minFilter=THREE.LinearMipmapLinearFilter;next.magFilter=THREE.LinearFilter;next.generateMipmaps=true;next.needsUpdate=true;setTexture(next);},undefined,()=>{if(!disposed)setTexture(null);});return()=>{disposed=true;loaded?.dispose();};},[url]);return texture;}
function PhysicalBook({volume,height,coverWidth,thickness,coverTexture,spineTexture,fallbackCover}:{volume:LibraryVolume;height:number;coverWidth:number;thickness:number;coverTexture:THREE.Texture|null;spineTexture:THREE.Texture|null;fallbackCover:THREE.Texture|null}){const hardcover=volume.binding!=='paperback';const boardThickness=hardcover?.022:.009;const overhang=hardcover?.028:.01;const pageHeight=Math.max(.2,height-overhang*2);const pageWidth=Math.max(.25,coverWidth-overhang*2-.014);const pageDepth=Math.max(.12,thickness-boardThickness*2-.012);const pageOffsetX=.014;const frontZ=pageDepth/2+boardThickness/2;const backZ=-frontZ;const frontOuterZ=frontZ+boardThickness/2;const spineRadius=pageDepth/2+boardThickness*.35;const spineX=-coverWidth/2+overhang*.55;const edgeRoughness=volume.jacket?.58:.94;const frontTexture=coverTexture??fallbackCover;return <group><mesh position={[pageOffsetX,0,0]} castShadow receiveShadow><boxGeometry args={[pageWidth,pageHeight,pageDepth]}/><meshStandardMaterial color="#e6dcc9" roughness={1}/></mesh><mesh position={[pageOffsetX+pageWidth/2+.001,0,0]} rotation={[0,Math.PI/2,0]}><planeGeometry args={[pageDepth*.95,pageHeight*.985]}/><meshStandardMaterial color="#d8cbb4" roughness={1}/></mesh><mesh position={[pageOffsetX,pageHeight/2+.001,0]} rotation={[-Math.PI/2,0,0]}><planeGeometry args={[pageWidth*.985,pageDepth*.95]}/><meshStandardMaterial color="#eee6d8" roughness={1}/></mesh><mesh position={[0,0,frontZ]} castShadow receiveShadow><boxGeometry args={[coverWidth,height,boardThickness]}/><meshStandardMaterial color={volume.spineColor} roughness={edgeRoughness}/></mesh><mesh position={[0,0,backZ]} castShadow receiveShadow><boxGeometry args={[coverWidth,height,boardThickness]}/><meshStandardMaterial color={volume.spineColor} roughness={edgeRoughness}/></mesh>{hardcover?<mesh position={[spineX,0,0]} scale={[.22,1,1]} castShadow receiveShadow><cylinderGeometry args={[spineRadius,spineRadius,height*.988,48,1,false]}/><meshStandardMaterial color={volume.spineColor} roughness={.94}/></mesh>:<mesh position={[spineX,0,0]} castShadow receiveShadow><boxGeometry args={[.018,height*.988,thickness*.965]}/><meshStandardMaterial color={volume.spineColor} roughness={.9}/></mesh>}{spineTexture?<mesh position={[-coverWidth/2-(hardcover?.006:.003),0,0]} rotation={[0,-Math.PI/2,0]} renderOrder={2}><planeGeometry args={[thickness*.82,height*.94]}/><meshBasicMaterial map={spineTexture} toneMapped={false} side={THREE.DoubleSide} polygonOffset polygonOffsetFactor={-1}/></mesh>:null}{frontTexture?<mesh position={[0,0,frontOuterZ+.008]} renderOrder={3}><planeGeometry args={[coverWidth*.958,height*.958]}/><meshBasicMaterial map={frontTexture} toneMapped={false} side={THREE.DoubleSide} polygonOffset polygonOffsetFactor={-2}/></mesh>:null}{hardcover?<mesh position={[-coverWidth/2+overhang+.021,0,frontOuterZ+.003]}><boxGeometry args={[.01,height*.88,.003]}/><meshStandardMaterial color="#1f1915" transparent opacity={.09} roughness={1}/></mesh>:null}{hardcover?<><mesh position={[spineX+.014,pageHeight/2-.004,0]}><boxGeometry args={[.035,.01,pageDepth*.7]}/><meshStandardMaterial color={volume.bandColor} roughness={.9}/></mesh><mesh position={[spineX+.014,-pageHeight/2+.004,0]}><boxGeometry args={[.035,.01,pageDepth*.7]}/><meshStandardMaterial color={volume.bandColor} roughness={.9}/></mesh></>:null}</group>;}
function BookMesh({book,selected,hovered,onHover,onChoose}:{book:ShelfBook;selected:boolean;hovered:boolean;onHover:(slug:string|null)=>void;onChoose:(volume:LibraryVolume)=>void}){const groupRef=useRef<THREE.Group>(null);const selectionProgress=useRef(selected?1:0);const hoverProgress=useRef(0);const{volume,height,coverWidth,thickness,shelfX,shelfZ}=book;const spineTexture=useSpineTexture(volume);const coverTexture=useCoverTexture(presentationArtUrl(volume));const fallbackCover=useFallbackCoverTexture(volume);const shelfY=SHELF_TOP+height/2;const lean=volume.lean*DEG;const shelfQuaternion=useMemo(()=>new THREE.Quaternion().setFromEuler(new THREE.Euler(0,Math.PI/2,lean)),[lean]);const hoverQuaternion=useMemo(()=>new THREE.Quaternion().setFromEuler(new THREE.Euler(0,Math.PI/2-7*DEG,lean*.65)),[lean]);const frontQuaternion=useMemo(()=>new THREE.Quaternion().setFromEuler(new THREE.Euler(0,0,0)),[]);const workingQuaternion=useMemo(()=>new THREE.Quaternion(),[]);useFrame((_,delta)=>{const group=groupRef.current;if(!group)return;const selectionAlpha=1-Math.exp(-5.6*delta);const hoverAlpha=1-Math.exp(-10*delta);selectionProgress.current+=((selected?1:0)-selectionProgress.current)*selectionAlpha;hoverProgress.current+=((hovered&&!selected?1:0)-hoverProgress.current)*hoverAlpha;if(Math.abs(selectionProgress.current-(selected?1:0))<.0005)selectionProgress.current=selected?1:0;if(Math.abs(hoverProgress.current-(hovered&&!selected?1:0))<.0005)hoverProgress.current=hovered&&!selected?1:0;const selectionT=selectionProgress.current;const hoverT=hoverProgress.current*(1-selectionT);const pullT=THREE.MathUtils.smoothstep(selectionT,0,.34);const faceT=THREE.MathUtils.smoothstep(selectionT,.34,1);const pullDistance=.28;const selectedZ=1.42;const selectedX=-1.02;group.position.set(THREE.MathUtils.lerp(shelfX,selectedX,faceT),shelfY+.006*hoverT+.014*pullT+.018*faceT,shelfZ+.075*hoverT+pullDistance*pullT+(selectedZ-pullDistance)*faceT);workingQuaternion.copy(shelfQuaternion).slerp(hoverQuaternion,hoverT);workingQuaternion.slerp(frontQuaternion,faceT);group.quaternion.copy(workingQuaternion);group.scale.setScalar(1);});const handlePointerOver=(event:ThreeEvent<PointerEvent>)=>{event.stopPropagation();onHover(volume.publicSlug);document.body.style.cursor='pointer';};const handlePointerOut=(event:ThreeEvent<PointerEvent>)=>{event.stopPropagation();onHover(null);document.body.style.cursor='';};return <group ref={groupRef} position={[shelfX,shelfY,shelfZ]} quaternion={shelfQuaternion} onPointerOver={handlePointerOver} onPointerOut={handlePointerOut} onClick={(event)=>{event.stopPropagation();onChoose(volume);}}><PhysicalBook volume={volume} height={height} coverWidth={coverWidth} thickness={thickness} coverTexture={coverTexture} spineTexture={spineTexture} fallbackCover={fallbackCover}/></group>;}
function CameraAim(){const{camera}=useThree();useEffect(()=>{camera.lookAt(0,1.5,0);camera.updateProjectionMatrix();},[camera]);return null;}
function ShelfScene({books,selectedId,hoveredId,onHover,onChoose}:{books:ShelfBook[];selectedId:string|null;hoveredId:string|null;onHover:(slug:string|null)=>void;onChoose:(volume:LibraryVolume)=>void}){return <><CameraAim/><hemisphereLight color="#fffaf1" groundColor="#73695f" intensity={.72}/><ambientLight intensity={.18}/><directionalLight castShadow position={[3.1,6.2,7.4]} intensity={1.12} shadow-mapSize-width={2048} shadow-mapSize-height={2048} shadow-camera-left={-4.5} shadow-camera-right={4.5} shadow-camera-top={4.5} shadow-camera-bottom={-1.2} shadow-bias={-.00028} shadow-radius={5}/><directionalLight position={[-4.2,3.8,5.2]} intensity={.2}/><mesh position={[0,-.006,-.015]} receiveShadow><boxGeometry args={[6.9,.065,.4]}/><meshStandardMaterial color="#b8aa94" roughness={.98}/></mesh><mesh position={[0,-.046,.16]} receiveShadow><boxGeometry args={[6.9,.028,.055]}/><meshStandardMaterial color="#9f8f79" roughness={.98}/></mesh>{books.map((book)=><BookMesh key={book.volume.publicSlug} book={book} selected={selectedId===book.volume.publicSlug} hovered={hoveredId===book.volume.publicSlug&&selectedId!==book.volume.publicSlug} onHover={onHover} onChoose={onChoose}/>)}</>;}
function buildShelfBooks(volumes:LibraryVolume[]){const dimensions=volumes.map((volume,index)=>{const height=volume.height/100;return{volume,height,coverWidth:height*(2/3),thickness:Math.max(.22,volume.width/100),shelfZ:((index%3)-1)*.012};});const gap=.016;const totalWidth=dimensions.reduce((sum,item)=>sum+item.thickness,0)+Math.max(0,dimensions.length-1)*gap;let cursor=-totalWidth/2;return dimensions.map((item)=>{const shelfX=cursor+item.thickness/2;cursor+=item.thickness+gap;return{...item,shelfX};});}
export default function ThreeBookshelf({volumes}:ThreeBookshelfProps){const router=useRouter();const books=useMemo(()=>buildShelfBooks(volumes),[volumes]);const[selectedId,setSelectedId]=useState<string|null>(null);const[hoveredId,setHoveredId]=useState<string|null>(null);const[detailVisible,setDetailVisible]=useState(false);const selectedVolume=useMemo(()=>volumes.find((volume)=>volume.publicSlug===selectedId)??null,[selectedId,volumes]);useEffect(()=>{setDetailVisible(false);if(!selectedId)return undefined;const timer=window.setTimeout(()=>setDetailVisible(true),650);return()=>window.clearTimeout(timer);},[selectedId]);useEffect(()=>()=>{document.body.style.cursor='';},[]);const chooseVolume=(volume:LibraryVolume)=>{if(selectedId===volume.publicSlug){if(!volume.demo)router.push(`/books/${volume.publicSlug}`);return;}setHoveredId(null);setSelectedId(volume.publicSlug);};return <section className={styles.stage} aria-label="Interactive bookshelf"><Canvas className={styles.canvas} shadows dpr={[1,1.75]} camera={{position:[.16,1.86,11.55],fov:23,near:.1,far:50}} gl={{alpha:true,antialias:true,powerPreference:'high-performance'}} onCreated={({gl})=>{gl.shadowMap.enabled=true;gl.shadowMap.type=THREE.PCFSoftShadowMap;gl.outputColorSpace=THREE.SRGBColorSpace;gl.toneMapping=THREE.ACESFilmicToneMapping;gl.toneMappingExposure=1.02;}} onPointerMissed={()=>{setSelectedId(null);setHoveredId(null);document.body.style.cursor='';}}><ShelfScene books={books} selectedId={selectedId} hoveredId={hoveredId} onHover={setHoveredId} onChoose={chooseVolume}/></Canvas><div className={`${styles.detail} ${detailVisible&&selectedVolume?styles.detailVisible:''}`}>{selectedVolume?<><div className={styles.kicker}>{selectedVolume.demo?'Binding study':selectedVolume.workType}</div><h2>{selectedVolume.title}</h2><p className={styles.creator}>{selectedVolume.creator}</p><p className={styles.facts}>{selectedVolume.demo?`${selectedVolume.binding} · temporary shelf example`:`${selectedVolume.chapterCount} chapters · ${selectedVolume.mediaCount} illustrations`}</p><button className={styles.returnButton} type="button" onClick={()=>{setSelectedId(null);setHoveredId(null);}}>Return to shelf</button></>:null}</div></section>;}
